"# InfiniteScroll" 
Infinite Scroll Component

// Task: Create a component that implements infinite scrolling for a list of items. The component should fetch more data when the user scrolls to the bottom of the list.

// Initial Requirements:

// Display a list of items (can be simple text items)
// Detect when user scrolls to bottom
// Load more items when bottom is reached
// Show loading indicator while fetching

// Potential Edge Cases:

// Handling API failures
// Preventing duplicate fetches
// Managing scroll position when new items are added
// End of data detection and handling

'https://dummyjson.com/products/?limit=10&skip=5'